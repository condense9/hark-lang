
def adder(x):
    def add(y):
        x + y
    add




# indent expr (NL expr)* dedent

# NL is the "expression separator"

# therefore, every expression must end with NL

# if indent...dedent is an expression, it needs a NL after it.

# everything is an expression

# a suite is one or more (expression followed by newline)
