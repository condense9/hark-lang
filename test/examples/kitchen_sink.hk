// Some basic examples

fn hello_int() {
  v = 5;
  print(v);
  v
}

fn hello() { "hello world" /* inline comment */ }

fn ident(x) {
  x
}

fn sq_strings() {
  // follows Python's string escaping semantics
  'hello \"quotes"'
}

fn escaped() {
  "hello \"escapes\""
}
